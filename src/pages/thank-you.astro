---
import Base from "../layouts/Base.astro";
const title = "Thank You | San Diego Commercial Mailbox";
const description = "We received your request and will follow up shortly.";
const jsonLd = {
  "@context": "https://schema.org",
  "@type": "WebPage",
  "name": "Thank You",
  "url": Astro.site ? new URL(Astro.url.pathname, Astro.site).toString() : undefined
};
---
<Base {title} {description} jsonLd={jsonLd}>
  <h1 class="text-3xl font-semibold mb-6">Thank you!</h1>
  <p class="opacity-80 mb-6">We’ll be in touch shortly.</p>
  <a href="/services" class="underline">Return to Services</a>
</Base>
