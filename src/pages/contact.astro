---
import LovableLayout from "../layouts/LovableLayout.astro";
import Base from "../layouts/Base.astro";
const title = "Contact | San Diego Commercial Mailbox";
const description = "Request a quote or schedule service.";
const jsonLd = {
  "@context": "https://schema.org",
  "@type": "ContactPage",
  "name": "Contact",
  "url": Astro.site ? new URL(Astro.url.pathname, Astro.site).toString() : undefined
};
---
<LovableLayout title="Contact | Postal Systems">
<Base {title} {description} jsonLd={jsonLd}>
  <h1 class="text-3xl font-semibold mb-6">Contact</h1>
  <p class="opacity-80 mb-6">Tell us what you need. We’ll respond quickly.</p>
  <div class="grid gap-6 max-w-xl">
    <a href="tel:5615109527" class="underline">Call 561-510-9527</a>
    <a href="mailto:info@example.com" class="underline">info@example.com</a>
    <a href="/services" class="underline">Browse Services</a>
  </div>
</Base>
</LovableLayout>
