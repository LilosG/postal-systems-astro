---
const { title = "Complete Mailbox Services, Done Right." } = Astro.props;
---
<section class="hero">
  <!-- COPY the exact hero HTML from your homepage here (image, badges, CTAs). -->
  <!-- Keep the same classes so Lovable CSS styles it identically. -->
</section>
