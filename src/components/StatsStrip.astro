---
const stats = [
  { value: "250+", label: "Installs" },
  { value: "100%", label: "USPS sign-off" },
  { value: "24–48h", label: "Service ETA" },
];
---
<section class="py-4">
  <div class="container mx-auto max-w-6xl px-4 grid gap-3 md:grid-cols-3">
    {stats.map((s) => (
      <div class="rounded-xl border border-slate-200 bg-white p-4 text-center">
        <div class="text-2xl font-bold">{s.value}</div>
        <div class="text-slate-500 text-sm">{s.label}</div>
      </div>
    ))}
  </div>
</section>
